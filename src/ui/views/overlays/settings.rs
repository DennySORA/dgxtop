use ratatui::Frame;
use ratatui::layout::Rect;

use crate::app::AppState;
use crate::ui::theme::Theme;

/// Render the settings overlay (placeholder for future implementation).
pub fn render(_frame: &mut Frame, _area: Rect, _state: &AppState, _theme: &Theme) {
    // Settings overlay will be implemented in a future iteration
}
